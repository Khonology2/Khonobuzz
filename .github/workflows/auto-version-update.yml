# Merged into deploy-check.yml as job auto-version-update.
name: Auto Version Control Update (standalone)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy-check workflow for push pipelines." && exit 0
