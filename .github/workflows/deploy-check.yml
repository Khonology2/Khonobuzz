name: deploy-check

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Deploy check paused
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "deploy-check is temporarily disabled while repository access is blocked."
          echo "The GitHub account suspension must be resolved before checkout-based workflows can run again."
