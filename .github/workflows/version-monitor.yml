# Schedule + behavior merged into deploy-check.yml (scheduled-maintenance job for cron; monitor steps on push).
# Keep file for documentation or local copy; push triggers removed to avoid duplicates.
name: Version Control Monitor (standalone)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hourly/daily schedules are in deploy-check.yml; use deploy-check for pushes."
          exit 0
