# Merged into deploy-check.yml as job generate-daily-commits (+ scheduled-maintenance for cron).
name: Generate Daily Commits (standalone)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy-check workflow for push pipelines." && exit 0
