# Merged into deploy-check.yml (push / workflow_dispatch). Run manually if needed in isolation.
name: Version Control (standalone)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is disabled for push; use workflow deploy-check instead."
          exit 0
