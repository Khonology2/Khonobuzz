name: deploy

on:
  workflow_dispatch:
jobs:
  disabled:
    name: Deploy pipeline paused
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "deploy is temporarily disabled while repository access is blocked."
          echo "Re-enable this workflow after the GitHub account suspension is resolved."
