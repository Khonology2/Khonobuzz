# Merged into deploy-check.yml as job version-api. Run manually if needed in isolation.
name: Version Control API (standalone)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is disabled for push; use deploy-check instead."
          exit 0
